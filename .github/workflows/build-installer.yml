name: Build and boot-test Finite installer ISO

on:
  schedule:
    - cron: "41 9 * * 0"
  workflow_dispatch:
    inputs:
      end-to-end:
        description: Also perform an unattended install and boot the installed system
        required: false
        default: false
        type: boolean
      image-tag:
        description: Published Finite profile to embed
        required: true
        default: bluefin-generic
        type: choice
        options:
          - bluefin-generic
          - bluefin-dell-xps-9350-intel
          - bluefin-dx-generic
          - bluefin-dx-dell-xps-9350-intel

concurrency:
  group: installer-${{ github.ref }}-${{ inputs.image-tag || 'bluefin-generic' }}
  cancel-in-progress: true

permissions:
  attestations: write
  contents: read
  id-token: write
  packages: write

jobs:
  build-installer:
    name: Build and boot installer ISO
    runs-on: ubuntu-24.04
    timeout-minutes: 120
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Build and smoke-test installer
        id: installer
        uses: ./.github/actions/build-installer
        with:
          cache-write: >-
            ${{ github.event_name == 'schedule' ||
            (github.event_name == 'workflow_dispatch' &&
            github.ref == 'refs/heads/main') }}
          end-to-end: ${{ github.event_name == 'schedule' || inputs.end-to-end }}
          image-tag: ${{ inputs.image-tag || 'bluefin-generic' }}
          token: ${{ github.token }}

      - name: Attest installer artifacts
        uses: actions/attest@1e69f48acb82d1966a394da916b4c1698aa569d6 # v4.2.2
        with:
          subject-path: |
            output/*.iso
            output/SHA256SUMS
            output/installer-manifest.json

      - name: Upload installer artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: finite-${{ steps.installer.outputs.payload-tag }}-installer
          path: |
            output/*.iso
            output/SHA256SUMS
            output/installer-manifest.json
            output/qemu-boot.log
          if-no-files-found: error
          retention-days: 14
