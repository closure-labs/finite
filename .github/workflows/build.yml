name: Build Purplefin

on:
  push:
    branches: [main]
  pull_request:
  merge_group:
    types: [checks_requested]
  schedule:
    - cron: "17 8 * * *"
  workflow_dispatch:
    inputs:
      force:
        description: Rebuild every image even when its inputs are current
        required: false
        default: false
        type: boolean
      validate_only:
        description: Validate a bot branch without publishing
        required: false
        default: false
        type: boolean

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' || github.event_name == 'push' }}

permissions:
  attestations: read
  contents: read
  packages: read

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  prepare:
    name: Check, classify, and plan
    runs-on: ubuntu-24.04
    timeout-minutes: 40
    permissions:
      contents: read
    outputs:
      base_image: ${{ steps.plan.outputs.base_image }}
      base_digest: ${{ steps.plan.outputs.base_digest }}
      base_tag: ${{ steps.plan.outputs.base_tag }}
      candidate_shards: ${{ steps.plan.outputs.candidate_shards }}
      hardware_matrix: ${{ steps.plan.outputs.hardware_matrix }}
      has_hardware: ${{ steps.plan.outputs.has_hardware }}
      has_builds: ${{ steps.plan.outputs.has_builds }}
      has_roles: ${{ steps.plan.outputs.has_roles }}
      has_root_base: ${{ steps.plan.outputs.has_root_base }}
      images: ${{ steps.filter.outputs.images }}
      installer: ${{ steps.filter.outputs.installer }}
      matrix: ${{ steps.plan.outputs.matrix }}
      role_matrix: ${{ steps.plan.outputs.role_matrix }}
      root_base: ${{ steps.plan.outputs.root_base }}
      version: ${{ steps.plan.outputs.version }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: ./.github/actions/setup-nix
        with:
          cachix-auth-token: ${{ secrets.CACHIX_AUTH_TOKEN }}
          github-token: ${{ github.token }}
          toolset: workflow-prepare
      - name: Validate repository and workflows
        env:
          CACHIX_AUTH_TOKEN: ${{ secrets.CACHIX_AUTH_TOKEN }}
          PURPLEFIN_CACHE_PUSH: ${{ github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository }}
        run: nix run .#ci
      - name: Select expensive validations
        id: filter
        env:
          DEFAULT_BRANCH_REF: origin/${{ github.event.repository.default_branch }}
          EVENT_NAME: ${{ github.event_name }}
          FORCE_REBUILD: ${{ github.event_name == 'workflow_dispatch' && inputs.force || false }}
          MERGE_GROUP_BASE_SHA: ${{ github.event.merge_group.base_sha }}
          MERGE_GROUP_HEAD_SHA: ${{ github.event.merge_group.head_sha }}
          PULL_REQUEST_BASE_SHA: ${{ github.event.pull_request.base.sha }}
          PULL_REQUEST_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          PUSH_AFTER_SHA: ${{ github.sha }}
          PUSH_BEFORE_SHA: ${{ github.event.before }}
          VALIDATE_ONLY: ${{ github.event_name == 'workflow_dispatch' && inputs.validate_only || false }}
        run: purplefin-classify-ci "${GITHUB_OUTPUT}"
      - name: Verify and plan immutable inputs
        if: steps.filter.outputs.images == 'true'
        id: plan
        env:
          CHECK_RPM_UPDATES: ${{ github.event_name == 'schedule' || github.event_name == 'workflow_dispatch' }}
          FORCE_REBUILD: ${{ github.event_name == 'workflow_dispatch' && inputs.force }}
          IMAGE_REF: ${{ format('{0}/{1}', env.REGISTRY, env.IMAGE_NAME) }}
        run: purplefin-ci-plan "${GITHUB_OUTPUT}"

  build-candidate:
    name: Validate shard ${{ matrix.shard }} (${{ matrix.name }})
    if: >-
      (github.event_name == 'pull_request' || github.event_name == 'merge_group' ||
      (github.event_name == 'workflow_dispatch' && inputs.validate_only)) &&
      needs.prepare.outputs.has_builds == 'true'
    needs: prepare
    strategy:
      fail-fast: false
      max-parallel: 4
      matrix: ${{ fromJSON(needs.prepare.outputs.candidate_shards) }}
    runs-on: ubuntu-24.04
    timeout-minutes: 120
    permissions:
      contents: read
      packages: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-nix
        with:
          github-token: ${{ github.token }}
          toolset: workflow-validation
      - name: Validate profiles from one locked base
        env:
          IMAGE_REF: ${{ format('{0}/{1}', env.REGISTRY, env.IMAGE_NAME) }}
          PROFILE_SHARD: ${{ toJSON(matrix.profiles) }}
          UPSTREAM_BASE_DIGEST: ${{ needs.prepare.outputs.base_digest }}
        run: purplefin-validate-image-shard

  installer-candidate:
    name: Validate installer
    if: >-
      (github.event_name == 'pull_request' || github.event_name == 'merge_group' ||
      (github.event_name == 'workflow_dispatch' && inputs.validate_only)) &&
      needs.prepare.outputs.installer == 'true'
    needs: prepare
    runs-on: ubuntu-24.04
    timeout-minutes: 150
    permissions:
      attestations: read
      contents: read
      packages: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: ./.github/actions/build-installer
        with:
          cache-write: false
          image-tag: base-generic-x86_64
          token: ${{ github.token }}

  base-publish:
    name: Build and publish common base
    if: >-
      (github.event_name == 'push' || github.event_name == 'schedule' ||
      (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main' && !inputs.validate_only)) &&
      needs.prepare.outputs.has_root_base == 'true'
    needs: prepare
    uses: ./.github/workflows/build-profile.yml
    permissions:
      attestations: write
      contents: read
      id-token: write
      packages: write
    with:
      build-input: ${{ fromJSON(needs.prepare.outputs.root_base).build_input }}
      parent-digest: ${{ needs.prepare.outputs.base_digest }}
      parent-from-lock: true
      parent-image: ${{ needs.prepare.outputs.base_image }}
      parent-tag: ${{ needs.prepare.outputs.base_tag }}
      profile: base
      publish: true
      reuse-existing: ${{ fromJSON(needs.prepare.outputs.root_base).reuse_existing }}
      tags: ${{ fromJSON(needs.prepare.outputs.root_base).tags }}
      upstream-base-digest: ${{ needs.prepare.outputs.base_digest }}
      version: ${{ needs.prepare.outputs.version }}

  hardware-publish:
    name: Build and publish ${{ matrix.profile }}
    if: >-
      always() && (github.event_name == 'push' || github.event_name == 'schedule' ||
      (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main' && !inputs.validate_only)) &&
      needs.prepare.result == 'success' && needs.prepare.outputs.has_hardware == 'true' &&
      (needs.base-publish.result == 'success' || needs.base-publish.result == 'skipped')
    needs: [prepare, base-publish]
    strategy:
      fail-fast: false
      max-parallel: 4
      matrix: ${{ fromJSON(needs.prepare.outputs.hardware_matrix) }}
    uses: ./.github/workflows/build-profile.yml
    permissions:
      attestations: write
      contents: read
      id-token: write
      packages: write
    with:
      build-input: ${{ matrix.build_input }}
      parent-digest: ${{ matrix.parent_digest || '' }}
      parent-image: ${{ format('ghcr.io/{0}', github.repository) }}
      parent-profile: ${{ matrix.parent }}
      parent-tag: ${{ matrix.parent_tag }}
      profile: ${{ matrix.profile }}
      publish: true
      reuse-existing: ${{ matrix.reuse_existing }}
      tags: ${{ matrix.tags }}
      upstream-base-digest: ${{ needs.prepare.outputs.base_digest }}
      version: ${{ needs.prepare.outputs.version }}

  roles-publish:
    name: Build and publish ${{ matrix.profile }}
    if: >-
      always() && (github.event_name == 'push' || github.event_name == 'schedule' ||
      (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main' && !inputs.validate_only)) &&
      needs.prepare.result == 'success' && needs.prepare.outputs.has_roles == 'true' &&
      (needs.base-publish.result == 'success' || needs.base-publish.result == 'skipped') &&
      (needs.hardware-publish.result == 'success' || needs.hardware-publish.result == 'skipped')
    needs: [prepare, base-publish, hardware-publish]
    strategy:
      fail-fast: false
      max-parallel: 4
      matrix: ${{ fromJSON(needs.prepare.outputs.role_matrix) }}
    uses: ./.github/workflows/build-profile.yml
    permissions:
      attestations: write
      contents: read
      id-token: write
      packages: write
    with:
      build-input: ${{ matrix.build_input }}
      parent-digest: ${{ matrix.parent_digest || '' }}
      parent-image: ${{ format('ghcr.io/{0}', github.repository) }}
      parent-profile: ${{ matrix.parent }}
      parent-tag: ${{ matrix.parent_tag }}
      profile: ${{ matrix.profile }}
      publish: true
      reuse-existing: ${{ matrix.reuse_existing }}
      tags: ${{ matrix.tags }}
      upstream-base-digest: ${{ needs.prepare.outputs.base_digest }}
      version: ${{ needs.prepare.outputs.version }}

  base-sbom:
    name: Attest common base software bill of materials
    if: >-
      always() && needs.prepare.result == 'success' && needs.prepare.outputs.has_root_base == 'true' &&
      needs.base-publish.result == 'success'
    needs: [prepare, base-publish]
    uses: ./.github/workflows/attest-software-bill-of-materials.yml
    permissions:
      attestations: write
      contents: read
      id-token: write
      packages: write
    with:
      build-input: ${{ fromJSON(needs.prepare.outputs.root_base).build_input }}
      profile: base
      tags: ${{ fromJSON(needs.prepare.outputs.root_base).tags }}
      upstream-base-digest: ${{ needs.prepare.outputs.base_digest }}
      version: ${{ needs.prepare.outputs.version }}

  hardware-sbom:
    name: Attest hardware software bill of materials for ${{ matrix.profile }}
    if: >-
      always() && needs.prepare.result == 'success' && needs.prepare.outputs.has_hardware == 'true' &&
      needs.hardware-publish.result == 'success'
    needs: [prepare, hardware-publish]
    strategy:
      fail-fast: false
      max-parallel: 4
      matrix: ${{ fromJSON(needs.prepare.outputs.hardware_matrix) }}
    uses: ./.github/workflows/attest-software-bill-of-materials.yml
    permissions:
      attestations: write
      contents: read
      id-token: write
      packages: write
    with:
      build-input: ${{ matrix.build_input }}
      profile: ${{ matrix.profile }}
      tags: ${{ matrix.tags }}
      upstream-base-digest: ${{ needs.prepare.outputs.base_digest }}
      version: ${{ needs.prepare.outputs.version }}

  role-sbom:
    name: Attest role software bill of materials for ${{ matrix.profile }}
    if: >-
      always() && needs.prepare.result == 'success' && needs.prepare.outputs.has_roles == 'true' &&
      needs.roles-publish.result == 'success'
    needs: [prepare, roles-publish]
    strategy:
      fail-fast: false
      max-parallel: 4
      matrix: ${{ fromJSON(needs.prepare.outputs.role_matrix) }}
    uses: ./.github/workflows/attest-software-bill-of-materials.yml
    permissions:
      attestations: write
      contents: read
      id-token: write
      packages: write
    with:
      build-input: ${{ matrix.build_input }}
      profile: ${{ matrix.profile }}
      tags: ${{ matrix.tags }}
      upstream-base-digest: ${{ needs.prepare.outputs.base_digest }}
      version: ${{ needs.prepare.outputs.version }}

  ci-gate:
    name: CI gate
    if: always()
    needs: [prepare, build-candidate, installer-candidate, base-publish, hardware-publish, roles-publish, base-sbom, hardware-sbom, role-sbom]
    runs-on: ubuntu-slim
    timeout-minutes: 5
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Require the complete event-appropriate build graph
        env:
          BASE_PUBLISH_RESULT: ${{ needs.base-publish.result }}
          BASE_SBOM_RESULT: ${{ needs.base-sbom.result }}
          BUILD_CANDIDATE_RESULT: ${{ needs.build-candidate.result }}
          EVENT_NAME: ${{ github.event_name }}
          HARDWARE_PUBLISH_RESULT: ${{ needs.hardware-publish.result }}
          HARDWARE_SBOM_RESULT: ${{ needs.hardware-sbom.result }}
          HAS_BUILDS: ${{ needs.prepare.outputs.has_builds }}
          HAS_HARDWARE: ${{ needs.prepare.outputs.has_hardware }}
          HAS_ROLES: ${{ needs.prepare.outputs.has_roles }}
          HAS_ROOT_BASE: ${{ needs.prepare.outputs.has_root_base }}
          IMAGES_SELECTED: ${{ needs.prepare.outputs.images }}
          INSTALLER_CANDIDATE_RESULT: ${{ needs.installer-candidate.result }}
          INSTALLER_SELECTED: ${{ needs.prepare.outputs.installer }}
          PLAN_RESULT: ${{ needs.prepare.outputs.images == 'true' && 'success' || 'skipped' }}
          PREPARE_RESULT: ${{ needs.prepare.result }}
          REF: ${{ github.ref }}
          ROLE_SBOM_RESULT: ${{ needs.role-sbom.result }}
          ROLES_PUBLISH_RESULT: ${{ needs.roles-publish.result }}
          VALIDATE_ONLY: ${{ github.event_name == 'workflow_dispatch' && inputs.validate_only || false }}
        run: automation/github/ci-gate.sh
