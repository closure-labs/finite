---
version: 1
configuration:
  ipa:
    # Finite's module adds the OV02C10 gain/black-level helper. It is
    # deliberately unsigned and therefore remains isolated by libcamera's
    # stock soft_ipa_proxy.
    module_paths:
      - /usr/lib64/libcamera/ipa-finite
