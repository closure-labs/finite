# Espanso's Wayland backend reads evdev keyboard events. Grant access only to
# the active local seat user; 73-seat-late.rules applies the resulting ACL.
SUBSYSTEM=="input", KERNEL=="event*", ENV{ID_INPUT_KEYBOARD}=="1", TAG+="uaccess"
