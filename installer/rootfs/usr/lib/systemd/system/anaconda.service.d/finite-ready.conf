[Service]
# anaconda.service is Type=forking. ExecStartPost runs only after its tmux
# session has been created successfully, giving CI a Finite-owned marker
# that is stronger than matching an arbitrary Anaconda boot message.
ExecStartPost=/usr/bin/sh -c '/usr/bin/printf "FINITE_INSTALLER_READY=1\n" >/dev/console'
